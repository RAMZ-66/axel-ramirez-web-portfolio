// Placeholder Express server for future use
// To use later:
// 1) npm init -y
// 2) npm install express cors multer sharp
// 3) Implement endpoints (e.g., /optimize-image)
// 4) Deploy separately; consume from Next.js via fetch()

/*
const express = require('express');
const cors = require('cors');
const app = express();
app.use(cors());
app.use(express.json());

app.get('/health', (_, res) => res.json({ ok: true }));

app.listen(4000, () => console.log('Express server on :4000'));
*/
